/// get_state(i , j)
/*
    return -1 if is invalid
    1 is alive
    0 is death
*/


if (is_valid_cell(argument[0], argument[1])) {
    return object0.live[# argument[0], argument[1]]    
}

return -1;
